name: Smoke4

# Isolates the tag filter pattern from release.yml.
on:
  push:
    tags:
      - "[0-9]+.[0-9]+.[0-9]+*"

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "tag glob matched and the workflow started"
